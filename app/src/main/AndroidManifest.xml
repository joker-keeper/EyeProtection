<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.EyeProtection"
        tools:targetApi="31">
        <service android:name=".camera.SlientCamera"
            android:foregroundServiceType="camera"/>
<!--        <activity-->
<!--            android:name=".optometry"-->
<!--            android:exported="false" />-->
        <!-- <activity -->
        <!-- android:name=".MainExercise" -->
        <!-- android:exported="false" -->
        <!-- android:theme="@style/LoginTheme"/> -->
        <activity
            android:name=".eye_exercise"
            android:exported="false"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".eyeSetting"
            android:exported="false"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".homePage"
            android:exported="false"
            android:theme="@style/LoginTheme" />
        <activity
            android:name=".login"
            android:exported="true"
            android:theme="@style/LoginTheme">

            <!-- intent-filter 是主activity需要的配置 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signUp"
            android:theme="@style/LoginTheme" />
    </application>

</manifest>